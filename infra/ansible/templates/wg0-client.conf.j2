# MacBook WireGuard config — managed by Ansible
[Interface]
Address = {{ macbook_wg_ip }}/24
PrivateKey = {{ macbook_wg_private_key }}

[Peer]
# VPS
PublicKey = {{ vps_wg_public_key }}
Endpoint = {{ vps_public_ip }}:{{ wg_port }}
AllowedIPs = {{ vps_wg_ip }}/32
PersistentKeepalive = 25
